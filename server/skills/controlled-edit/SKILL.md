---
name: controlled-edit
description: 只修改用户授权变化的视觉维度，并锁定其余核心主体。
---

# 受控局部编辑

只改变用户明确允许变化的维度。人物身份与五官、服装款式与材质、商品结构与包装、姿态、构图、光线等未授权变化的核心维度必须保持。

把锁定项明确写进 Prompt 和 `preserve` 约束，把允许变化项写成 `vary`。新结果创建独立分支，不覆盖父图；无法确认的关键锁定关系应进入 Creative Brief，不得自行猜测。
