---
name: root-recipe-redo
description: 从根配方创建独立重做分支，避免结果图逐代污染。
---

# 按原参数重做

从根配方恢复原始参考、Prompt 和生成参数，创建独立首图分支。当前结果图不能作为直接输入，也不能覆盖既有分支。

仅应用用户本轮明确提出且通过兼容性校验的修改；其余内容沿用根配方。若根配方缺失，应明确失败，不能退化为以当前结果继续生成。
