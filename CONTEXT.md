# Botanic Canvas Context

## Ubiquitous language

- **素材节点**：代表一个可被连接到生成节点的品牌、上传或已选输出媒体；媒体可以是图片或视频。
- **素材合集**：素材的可选分组路径；文件夹上传保留相对路径，缺省素材仍归入“全部”。
- **全局品牌素材库**：跨项目共享的内置/品牌素材集合；它不是任何项目的画布状态，新项目创建后即可使用其中素材。
- **项目私有素材**：某一项目上传或生成的素材；只属于该项目的素材库、画布、模板与历史，不能自动出现在其他项目中。
- **文本节点**：代表一段可被连接到生成节点的创意描述。
- **生成节点**：一次可配置、可执行的图像生成意图；它拥有提示词、模型、比例、画质与候选数量。
- **输入连线**：从素材、文本或已选图片指向生成节点的连线；它是该次生成唯一的输入来源。
- **主商品**：一个生成节点输入中的商品素材，用于锁定本次生成的商品主体。
- **输出媒体节点**：一次任务返回的每张图片或视频都会立即成为独立、可复用的画布节点；可作为下一个生成节点的输入，也可直接删除。
- **Frame**：画布上的可命名组织区域；成员节点可归属于一个 Frame，删除 Frame 只解除成员归属。
- **Stage**：Frame 对应的生产阶段分类，用于把需求、参考、生成、审阅、批准与交付组织成泳道。
- **分支**：从一张已选输出图片发起新的生成节点与任务，不覆盖父图片。
- **工作流模板**：不含已执行任务与候选图的画布蓝图，用于开始新的创作。
- **运行历史**：一次已确认输出及其当时输入的不可变记录。
- **Agent 会话**：项目内的一段连续创作对话；它拥有标题、执行模式和当前上下文，但不拥有消息内容本身。
- **Agent 消息**：Agent 会话中的独立交流记录；同一消息标识只能属于一个项目和一个会话。
- **Agent Turn**：一次对话控制权循环的可恢复记录；用租约与 fencing 保证单执行者，事件序号是断线续读游标。它只引用 Message、Run、Job 的业务事实，不复制它们。
- **botanicAgent\* / agent\* 前缀**：`botanicAgent*` 是产品语义解析层（Prompt、意图、计划、画布语义）；`agent*` 是通用 Agent 控制面（Turn 生命周期、Tool Loop、Context、恢复、协议、指标）。新模块按此裁决命名；存量触达时对齐，不做机械全局改名。
- **Plan**：把用户意图、锁定约束、可变维度和分支组织成待确认的执行方案；确认后产生 Agent Run。
- **Skill**：项目内已审核、版本化的执行契约；每个版本的完整快照不可覆盖，Run 与挂载按版本和内容摘要绑定。
- **Action Receipt**：不可重复行动（Skill / MCP / 计费操作）的执行回执；同一回执标识只表达同一行动意图，结果未知时不自动重试。
- **Creative Brief**：Prompt 或生成前逐轮补全的结构化创作约定；记录交付规格、创作方向、保持重点及每项来源，并随追问和计划持久化。
- **项目记忆**：项目成员确认后长期保留的创作规则、认可方向或避免事项；删除后的记忆不能被旧项目快照重新带回。
- **Agent Run**：用户确认计划后产生的可恢复执行记录；它拥有分支、任务进度和最终状态。
- **Artifact**：Agent 行动或生成任务产生的可复用结果，可以是媒体、文本、工作流、素材组或文件，并保留产生它的行动、任务与画布血缘。
- **Artifact Index**：项目内全部历史 Artifact 的持续目录；画布节点删除后仍保留历史身份与来源，但它本身不是素材库。
- **已删除素材**：已从资产库撤销的素材；项目私有素材只清理当前项目，全局品牌素材会同步清理所有项目中的后续可复用引用，二者都不得再作为后续任务输入。
