# Botanic 代码地图

这份地图回答三个问题：需求属于哪里、从哪个接口进入、改完验证什么。文件名表达所有权；同一行为只保留一个权威实现。

## 快速定位

| 需求/行为 | 首要入口 | 相关实现 | 聚焦测试与不变量 |
| --- | --- | --- | --- |
| 画布节点与连线 | `src/domain/canvas.ts` | `generationRecipe.ts`、`generateNodeCreation.ts`、`canvasNodeLayout.ts` | 对应 `src/domain/*.test.ts`；输入连线是生成配方唯一来源 |
| 输出节点与血缘 | `src/domain/generationOutputPlacement.ts` | `src/store/canvasGenerationProjection.ts`、`server/generationResultReconciliation.mjs`、`canvasPresentation.ts` | 每个输出独立成节点；候选 ID 稳定 |
| 应用登录与入口 | `src/App.tsx` | `src/lib/productSession.ts`、功能模块按需加载 | 应用壳不拥有画布或领域规则 |
| 前端发布门闩 | `src/lib/releaseGate.ts` | Vite 写出 `/release.json`、`src/App.tsx` 全屏刷新层、账户菜单只读版本号 | `src/lib/releaseGate.test.ts`；比较 `revision`，缺字段/拉取失败 fail-open；展示号来自 `package.json`；不走 `/api/health` |
| 画布交互与面板 | `src/features/canvas/CanvasWorkspace.tsx` | `CanvasEditorViews.tsx`、`CanvasWorkspacePanels.tsx`、`workspaceProjectCoordinator.ts`、`useCanvasWorkspaceSynchronization.ts`、`useCanvasAgentExecutionBridge.ts`、`useCanvasInteractionCoordinator.ts`、`canvasGenerationInteraction.ts`、`canvasWorkspaceNavigation.ts` | `src/features/canvas/*.test.ts`、UI E2E；项目 I/O、同步、Agent 执行桥、React Flow 交互与 4K 分支提交分别归对应协调器，工作区只组合导航与面板 |
| Agent 面板交互 | `src/features/agent/AgentWorkspace.tsx` | `AgentConversationMessage.tsx`、`AgentComposer.tsx`、`AgentUtilityPanels.tsx`、`useAgentMessageDelivery.ts`、`useAgentRuntimeTrace.ts`、`src/domain/agentMessageUtilities.ts`（赞踩/复制/编辑出现资格） | Agent 领域/Lib 测试；工作区只编排，对话卡和 Composer 各自拥有展示交互；元操作不占行，通知与任务回执不挂赞踩复制 |
| 界面动效 | `src/components/gsapMotion.ts` | `ProductLanding.tsx`、`AgentComposer.tsx`、`AgentWorkspace.tsx`、`AgentConversationMessage.tsx`、`LiquidProgressBar.tsx`、`liquidProgressShader.ts`、`generationLiquidTravel.ts`、`AgentToolOrb.tsx`、`AgentThinkingOrb.tsx` | `src/components/gsapMotion.test.ts`、`LiquidProgressBar.test.ts`、`generationLiquidTravel.test.ts`、`AgentThinkingOrb.test.ts`、`src/domain/agentTimeline.test.ts`；只动 transform/opacity；工具面板用 Flip（simple，不插值宽高）；结果节点 liquid 单次从左到右，跟任务阶段和 `submittedAt` 对齐，不循环、不接线 `generationProgress`、不伪造业务百分比；工具行进行中只换 thinking-orbs 的 state 动画，标题仍走 presentation；不伪造工具进度；reduced-motion 时长为 0 |
| Agent Composer 引用字段 | `src/domain/agentMentions.ts`、`readBotanicAgentMentionQuery`（`agent.ts`） | `AgentComposer.tsx`、`AgentConversationMessage.tsx`、`AgentPromptResponse.tsx`、`botanicAgentPersistence.mjs` | `/` 挂载 Skill、`@` 引用画布节点或图片视频；选中后写入 `mentions` 芯片，不写入可执行 Prompt |
| Agent 回合解析 | `server/botanicAgentTurn.mjs` | `server/agentThreadContext.mjs`（由 Session/Message 权威实体投影最近窗口）、`agentContextBudget.mjs`、`AgentWorkspace.tsx`、`agentApi.ts`、`botanicAgentWebTools.mjs` | 新客户端只提交 `sessionId + inputMessage`，不上传历史；相同 Message ID 以服务端记录为准；Summary+窗口 8k、Summary 2k、最近 16 条，当前输入超限 413；工具结果单条 2k/单轮 6k；Provider 明确 context overflow 时只在同一 model step、工具执行前严格裁剪重试一次；回合与对话共用 `web_search`/`web_fetch`（需 `BOTANIC_WEB_SEARCH_API_KEY`）；原始推理仅 `AGENT_RAW_REASONING` 下发且不落盘 |
| Agent Turn Runtime | `server/botanicAgentTurnRuntime.mjs` | `agentRuntimeRequest.mjs`（intent/chat/plan 单一 dispatcher）、`agentOperationalReaders.mjs`（API/Worker 同源只读工具）、`agentTurnRequestIdentity.mjs`（请求 binding 与 legacy fail-closed）、`agentTurnCheckpoint.mjs`、`agentEntityReferences.mjs`、`botanicAgentPersistence.mjs`（兼容写剥离 Message 引用）、`turnReclaim.mjs`、`agentTurnSweep.mjs`、`agentCancellationService.mjs`、`agentRoutes.mjs`、`worker.mjs`、`src/domain/agentTurnObservation.ts`、`src/domain/agentMessagePersistence.ts`、`src/lib/agentApi.ts` | `botanicAgentTurnRuntime.test.mjs`、`agentRuntimeRequest.test.mjs`、`agentTurnCheckpoint.test.mjs`、`agentEntityReferences.test.mjs`、`botanicAgentPersistence.test.mjs`、`turnReclaim.test.mjs`、`agentCancellationService.test.mjs`、`agentRoutes.test.mjs`、`httpServer.test.mjs`、`agentTurnObservation.test.ts`、`agentMessagePersistence.test.ts`、`agentMessageQueue.test.ts`、`scripts/agentTurnExecutionContract.test.mjs`、`agentTurnRuntimeMigration.test.mjs`；全部 Agent Provider 入口先进入 durable Turn，旧 URL 只做兼容 presenter；Plan/Chat 也有 operation 快照、Checkpoint、accepted/observer、稳定 key 与深取消；浏览器先 durable pending Message+完整快照，服务端再 claim/binding→Message link→accepted/202；queued 后原子 lease/fencing，prepared Checkpoint 必须先于副作用；刷新按 turnId/sequence 重挂并用稳定结果消息投影；联网工具来源经服务端公网校验后以有界 presentation 随 Turn Event 持久化，客户端恢复只解析有界 HTTPS 展示形状；引用只取受信工具固定路径（8/工具、24/Turn）并 sticky 合并；CanvasDocument 兼容写不能首次绑定引用；身份未返回时 pending/Stop 意图跨刷新保留；Stop 走深取消且 observer 保留；旧执行者不能覆盖接管者终态 |
| 画布应用状态 | `src/store/canvasStore.types.ts` | `canvasStore.ts`、`canvasDocumentLifecycleActions.ts`、`canvasAssetGraphActions.ts`、`canvasGenerationActions.ts`、`canvasGenerationLifecycle.ts`、`canvasGenerationProjection.ts`、`canvasTemplateHistoryActions.ts`、`canvasAgentActions.ts`、`canvasBatchVariationActions.ts` | 先核对 Store 端口；文档、图谱素材、普通生成、模板/历史、Agent 和批量变体命令分别由深模块拥有；远端新结果不得被旧草稿覆盖 |
| 普通生成任务 | `src/lib/generationApi.ts` | `server/generationService.mjs`、`generationProcessor.mjs`、`generationProviderAdmission.mjs`、`generationProvider.mjs`、`flockGenerationProvider.mjs`、`generationVocabulary.mjs`、`generationJobExecution.mjs`、`generationJobCas.mjs`、`generationOutputSize.mjs`、`generationComposition.mjs` | `server/generation*.test.mjs`、`flockGenerationProvider.test.mjs`、`generationJobExecution.test.mjs`、`generationProcessorFence.test.mjs`、`generationJobAdapterContract.test.mjs`、`src/domain/generationOutputSize.test.ts`、`src/domain/generationComposition.test.ts`、`scripts/generationVocabularyContract.test.mjs`；同一次重试复用幂等键；Job 先 claim generation/lease，进度与终态 fenced commit；租约接管遇本机旧 handle 时先 abort 并 fail-safe 退出，不触发第二次 Provider 调用，留待 lease 恢复；Job 终态先于 Canvas/Artifact/Run 投影；旧 generation 与旧 activeJobId 不得覆盖 retry；Flock 在输入物化前取得可取消的进程级高内存许可，fallback 真正进入时才加锁；Provider 图片允许 32MB 存储与再输入，用户上传仍为 8MB、单任务总输入仍为 48MB；gpt-image-2 可自定义像素；有 Flock key 时目录含 Nano Banana，4K 只经「4K」按钮写入；多图合成时标识不得当底图 |
| Generation 恢复扫描 | `server/generationRecoverySweep.mjs` | `updatedAtIdSweepCursor.mjs`、三个 ProductStore Adapter、`worker.mjs`、`supabase/migrations/20260827160000_agent_recovery_pagination.sql`、`20260828150000_agent_recovery_keyset.sql` | `generationRecoverySweep.test.mjs`、`recoveryAdapterContract.test.mjs`、`scripts/agentRecoveryKeysetMigration.test.mjs`、`agentRecoveryPaginationContract.test.mjs`、`generationRecoveryWorkerContract.test.mjs`；毫秒时间+ID keyset、有界页数与尾部回绕扫描，坏页停滞保护、逐 Job 入队隔离；Supabase 四类恢复记录用全量写 trigger 从 `updated_at` 维护 `recovery_updated_at_ms`，RPC 以首屏/续页双静态分支让复合行 cursor 与 `(recovery_updated_at_ms,id COLLATE "C")` partial index 同键，generic plan 深页也直接从索引位置起跳；Worker 分别扫描失败 Run 分支、待 Review 与可恢复 Job，不让单个毒任务饿死后续任务 |
| 生成成本与 Provider 容灾 | `server/generationGovernance.mjs` | `securityControls.mjs`、`providerHealthMonitor.mjs`、`generationRoutes.mjs`、`generationProcessor.mjs` | 任务级唯一记账；多维预算原子预留；仅语义兼容模型可降级；熔断半开后恢复 |
| 线程摘要与执行冻结 | `server/agentThreadSummary.mjs` | `agentThreadContext.mjs`（权威窗口、旧消息分页与 CAS 检查点写回）、`agentContextBudget.mjs`、`agentToolRuntime.mjs`（`freezeAgentStepSnapshot`）、`botanicAgentTurn.mjs`（摘要以低权限用户上下文注入）、`agentRoutes.mjs`、`botanicAgentPersistence.mjs`、三个 ProductStore Adapter | `server/agentThreadSummary.test.mjs`、`agentThreadContext.test.mjs`、`agentThreadSummaryCasContract.test.mjs`、`agentToolRuntime.test.mjs`；四类上下文层不得混用；摘要以 `factCandidates + coveredMessageRevisions` 保留逐 Message 来源，同 ID revision 修订/撤回先移除旧事实；legacy 摘要只在完整有界历史下重建；首次摘要稳定分页最多回填 500 条，失败不持久化残缺摘要；CAS 只 patch `threadSummary` 子字段且不改变 Session 排序时间；Artifact 只存 id/kind/label，业务引用只来自稳定 Turn 助手投影；不含 Prompt、媒体地址、链接与凭据；工具集在循环前定格 |
| Agent Context Compaction V2 | `server/agentModelContextSurface.mjs`、`server/agentContextCoordinator.mjs` | `agentModelContextPolicy.mjs`、`agentTokenMeter.mjs`、`agentContextCompaction.mjs`、`agentModelContextRuntime.mjs`、`agentContextPersistence.mjs`、`agentThreadContext.mjs`、`agentToolRuntime.mjs`、三个 ProductStore Adapter、`agentRoutes.mjs`（`POST /api/projects/:projectId/agent-sessions/:sessionId/context-compactions`）、`supabase/migrations/20260828210000_agent_context_compaction_v2.sql` | `agentModelContext*.test.mjs`、`agentTokenMeter.test.mjs`、`agentContext*.test.mjs`、`agentThreadContext.test.mjs`、`agentToolRuntime.test.mjs`；`agent_messages` 始终是权威且不删除，Surface 公开层只含哈希/计数；精确 model policy → default → legacy 8k input 安全回退；state + append-only transition ledger 用 DB-clock CAS，同 key 同 payload 可在 head 推进后 replay，`expectedRevision` 不进请求哈希；V1/V2 Snapshot 共存；manual 端点要求 Editor + `Idempotency-Key`、强制 `trigger=manual` 且不调用 Provider；Provider body/raw reasoning 禁止持久化；Flag 默认关闭，staging 先迁移并通过多实例门禁 |
| 分支重试与工作流发布 | `server/agentBranchRetryService.mjs`、`server/productionWorkflowPublishService.mjs` | `agentBranchRetryPolicy.mjs`、`agentBranchRetrySweep.mjs`、`agentRoutes.mjs`、`productionWorkflowRoutes.mjs`、`worker.mjs`（`branch.retry`） | `server/agentBranchRetry*.test.mjs`；两者各只有一份实现，路由与运维工具共用 —— 重试复制出错等于重复扣费，发布复制出错等于同一次发布固定出不同的不可变契约；自动重试是白名单（未知错误码不重试）、只重一次、高成本与预算不足停下并记录原因 |
| Agent 运维工具 | `server/botanicAgentOperationalTools.mjs` | `botanicAgentTurn.mjs`（只读工具接入回合）、`botanicAgentTools.mjs`（写工具进确认注册表）、`agentActionGovernance.mjs`（权限表）、`agentRoutes.mjs`（读取器与执行器） | `server/botanicAgentOperationalTools.test.mjs`、`agentRoutes.test.mjs`；只返回结构化状态不拼文案；**不返回受控媒体地址、Prompt 或 Provider 原始回包**；缺读取器/执行器或权限不足就不暴露（Viewer 看不到写工具）；暴露判定与服务端权限表同源，有测试逐角色比对 |
| Agent 行动执行与人工调和 | `server/agentActionExecution.mjs`、`server/agentActionReconciliation.mjs` | `agentRoutes.mjs`（`/api/agent-actions/status|resolve`）、`agentToolRuntime.mjs`（执行前已知失败标记）、`mcpClient.mjs`（取消与 intent header）、`productStoreContract.mjs`、三个 ProductStore Adapter、Action claim/settle/resolve/consume RPC、`src/domain/agent.ts`、`agentActionMessagePersistence.ts`、`agentMessageQueue.ts`、`AgentWorkspace.tsx` | `server/agentActionExecution.test.mjs`、`agentActionReconciliation.test.mjs`、`postgresAgentActionReconciliation.test.mjs`、`agentRoutes.test.mjs`、`scripts/agentActionExecutionContract.test.mjs`、`agentActionReconciliationContract.test.mjs`、`agentActionApprovalContract.test.mjs`、`agentActionMessagePersistence.test.ts`、`agentMessageQueue.test.ts`；副作用前原子 claim；状态查询不执行；人工确认生效不伪造结果；确认未生效前先持久化新 key，v2 Store 原子预留对应 retry Receipt 且不下发 raw token，fresh approval 后原子消费；consume→claim 崩溃只允许同 consumedByReceiptId 显式恢复；retry 再次未知后耗尽且不签第二次；v1 token 只在组件内存兼容；Local 仅保证单进程，数据库 Adapter 才保证多实例 |
| 结果评审与人工决定 | `server/agentReviewTask.mjs` | `agentReviewDeterministic.mjs`、`agentReviewVision.mjs`、`agentReviewRunner.mjs`、`agentReviewExecution.mjs`、`agentReviewReconciliation.mjs`、`agentReviewService.mjs`、`agentReviewDecisionService.mjs`、`agentReviewRetryMaterialization.mjs`、`mediaSpec.mjs`、三个 ProductStore Adapter、`derivedTaskQueue.mjs`（`review.run` / `run.submit`）、`agentRoutes.mjs`、`agentRunEventBus.mjs` | `server/agentReview*.test.mjs`、`agentReviewExecutionAdapterContract.test.mjs`、`agentReviewRetrySupabaseContract.test.mjs`、`mediaSpec.test.mjs`；评审用 claim/lease/generation/prepared/result fence，prepared 未知结果不自动重跑；停止先写 generation-fenced `cancelling`，只有 Worker exit 或 DB lease expiry 才收口 `cancelled`；outcome_unknown 只能人工选择 truthful `continue_unverifiable` 或至多一次、显式记录重复调用风险的 `retry_once`；公共 DTO 不含 owner/lease/signal/idempotency/prior；硬规格 fail 不进视觉层，rubric 来自固定质量策略；自动结论只待人工且不改 Run/Job；accept/reject 走 `review_decide`，retry 走需生成权限和确认的 costly `review_retry`；Human Decision + retryMaterialization + 稳定 queued Run 原子提交，批量冲突整体零写，事务内不调 Provider，`run.submit` 后续建 Job；legacy retry 无物化身份 fail closed；界面只渲染领域展示规则 |
| 交付包打包 | `server/zipArchive.mjs`、`server/deliveryPackage.mjs` | `deliveryManifest.mjs`（装哪些文件完全由清单决定）、`productionWorkflowRoutes.mjs`（`/production-workflow-runs/:id/package`）、`mediaService.readGenerationInput`（带项目归属校验）、`src/lib/productionWorkflowApi.ts` | `server/zipArchive.test.mjs`、`deliveryPackage.test.mjs`；仓库无 zip 依赖故手写，与 `mediaSpec`/`imageOverlay` 同一路数，但更危险 —— 中央目录写错的表现**不是报错，是交付给客户的包损坏**，因此测试里的读取器按规范独立重写（用自己的读法验自己的写法只能证明两边一致），并已用 Python `zipfile.testzip()` 与系统 `unzip -t` 交叉验证；**只用 store 不压缩**（交付物本身已压缩，deflate 收益≈0，却引入压缩流状态与 data descriptor 一整类可写错的东西）；逐文件缓冲因此 CRC 与长度在写头前就已知，峰值内存是最大单文件而非整包；条目数/大小/偏移越过 32 位边界自动切 ZIP64（视频批量很容易超 4GB，静默溢出会得到字段回绕、看似正常却解不开的包）；置 UTF-8 标志位否则中文名乱码；Zip Slip 与同名条目**报错而非自动改名**（改名会让文件名与清单对不上）；**取不到字节让整个包失败**，少几张的包比报错糟得多 —— 报错能重试，收到包的人不会去数够不够；清单本身以 `manifest.json` 进包；响应头在写第一个字节前发出，因此清单级冲突必须在开始写之前暴露，写到一半失败只能 `destroy` 连接而不是正常结束（正常结束会让客户端拿到「下载成功」的截断 zip） |
| Campaign 矩阵与跨输出一致性 | `src/domain/campaignMatrix.ts`、`server/campaignConsistency.mjs` | `src/domain/workflowBatchInput.ts`（CSV 导入，同一批量项形状）、`deliveryManifest.mjs`（清单带一致性结论）、`productionWorkflow.mjs` | `src/domain/campaignMatrix.test.ts`、`server/campaignConsistency.test.mjs`、`deliveryManifest.test.mjs`；张数必须在提交**前**能算出（`campaignMatrixSize` 不展开即可给出），超上限**拒绝并说明**而不是截断到上限（截断会让用户以为整批都提交了）；重复取值去重但报告，否则同一业务标识出现两次、「只重试失败的 2 项」会对不上号；Reference Pack 与品牌上下文全批共享而不是逐项复制；一致性 Gate 只查**能被证明**的维度（参考、品牌指纹、计划指纹、模型），比例与分辨率按设计本来就会变、默认不参与判定，否则每个正常 Campaign 都报警；缺记录判「无法验证」而不是默认通过；不一致时给出分成了哪几组、各含哪些 Artifact；Gate **报告而不阻断**出包，但清单上必须写明 |
| 品牌规则（Brand Kit） | `server/brandKit.mjs`、`src/domain/brandKitPresentation.ts`（界面展示） | `botanicCreativePlanCompiler.mjs`（编译进执行契约 + 质量策略）、`creativePlanResolver.mjs`（`resolveRunBrandKit`，Resolve 侧读权威状态）、`agentReviewVision.mjs`（逐条判据带规则原文）、`agentReviewTask.mjs`（品牌判据进策略指纹）、`libraryRoutes.mjs`（`/api/brand-kits`、`/api/projects/:id/brand-kit`）、`agentRunGenerationService.mjs` | `server/brandKit.test.mjs`、`brandKitPipeline.test.mjs`、`src/domain/brandKitPresentation.test.ts`；三层按 `global < project < run` 就近覆盖，`facet + key` 是槽位，同层同槽位重复**报错**不静默保留其一；多品牌按 `brandId` 硬校验，绑错品牌报错而不是过滤成空品牌；手册解析产出一律 `proposed`，`document_import` 未经确认不得激活，判不出维度的留空 `facet` 因而无法通过校验；`must` 不满足才判不合格、`should` 不满足是让步，该判定只有 `isBrandConcession` 一份实现，评审汇总与品牌 QA 摘要共用；品牌规则进计划指纹与评审任务指纹，但**无品牌判据时该键整个缺席**，否则存量策略指纹全变、已评审完的 Run 会再评一次；界面不重算覆盖优先级，解析一律由服务端做 |
| 项目自定义评审判据（evaluator Skill） | `server/agentReviewSkillEvaluator.mjs` | `botanicAgentSkill.mjs`（`manifest.kind` + `outputSchema`）、`botanicCreativePlanCompiler.mjs`（编译期固定进 `qualityPolicy.evaluatorSkills`）、`agentReviewRunner.mjs`、`agentReviewTask.mjs`（进策略指纹）、`worker.mjs`、`src/domain/agentReviewPresentation.ts` | `server/agentReviewSkillEvaluator.test.mjs`、`botanicAgentSkill.test.mjs`；这是 `outputSchema` 的消费方 —— Skill 只有 guidance 一种形态时它确实没有消费方，加了 `evaluator` 形态才成立；**每条判据 × 每个候选 = 一次额外模型调用**，因此数量硬上限 3 且成本必须在评审**开始前**就显示（评审完再说钱已经花掉）；判据标识**带 Skill 版本**（`skill.<id>@<version>`），Skill 版本不可变，不带版本的话改版后旧结论会看起来像新规则判出来的；在**编译期**固定（与品牌规则同一时点），之后新发布的 Skill 不回头评判已跑完的 Run —— 否则「结果符合用户确认的约束」变成移动靶；只有 `published` 的 Skill 能当判据（draft 没人批准过，不该决定结果合不合格）；跑在子任务治理里（预算/超时/白名单/Schema/可追踪终止），一条判据配错只让**这一条**记 unverifiable 而不是让整个评审失败；`guidance` 形态**禁止**带 outputSchema（声明一个没人校验的输出形状会让人以为它被校验过）；evaluator 的 outputSchema 必须把 `verdict` 列为必填且取值限于 pass/fail/unverifiable，否则评审层既没法汇总也没法展示 |
| Durable Subagent Runtime | `server/agentSubagentService.mjs`、`server/agentSubagentPersistence.mjs`、`server/agentSubagentProcessor.mjs` | `agentSubagentBroker.mjs`（Planner→Durable bridge）、`agentSubagentRunner.mjs`（多步只读执行器）、`agentSubagentQueue.mjs`（独立队列）、`agentSubagentRecovery.mjs`、`agentSubagentCancellation.mjs`、`agentSubagentRegistry.mjs`、`botanicAgentTools.mjs`（`subagent_research`）、三个 ProductStore Adapter、`worker.mjs`、`runtime.mjs`（`AGENT_SUBAGENT_MODEL/CONCURRENCY`） | `agentSubagent*.test.mjs`、`agentCancellationService.test.mjs`、`productStore.test.mjs`、`agentSubagentAdapterContract.test.mjs`；Descriptor 固定模型/指令版本/只读工具/Schema/预算，Activation 按 sequence gapless FIFO；每次 Activation 独立使用 Session/Message/Durable Turn，主 Planner 重放同一 Subtask ID 时复用结果；running 根 Turn 派发必须匹配当前 generation+lease，旧 executor takeover 后不能新增子任务；外部 start 只接受已结束根 Turn；根 Turn 取消按 `rootTurnId` 分页级联全部 Subagent，未确认子项终态前根 Turn 保持 cancelling；普通 Session 列表默认隐藏子会话，专用资源可读安全提案；public DTO 不含 owner/hash/lease/signal/raw reasoning；未显式配置模型时正式入口不注册派发工具，绝不回退进程内执行器 |
| 画布区域租约与候选终态 | `server/canvasRegionLease.mjs` | `agentSubtask.mjs`（多 Agent 并发编排）、项目文档 `writeProject` 的版本条件更新 | `server/canvasRegionLease.test.mjs`；租约是**咨询性**的，不替代持久化层的条件更新（只靠内存租约在多进程部署下失效）；区域标识由排序后的节点集合派生，换个书写顺序拿不到「另一个区域」；相交即冲突、不相交可并行（整文档版本冲突会让不相干的改动白跑）；同一持有者重复申请是续期不是冲突（重试是正常路径）；只有持有者能释放；落地前版本漂移返回 `requiresRevalidation` 而不是直接失败 —— 直接失败会把「有人改了画布另一角」变成「你这次编排作废」；候选终态不是文档写入，因此单独按 (候选, 决定) 幂等：同一决定重放是无操作，不同决定冲突 |
| 项目能力模型（前端隐藏无权限入口） | `src/domain/projectCapabilities.ts` | `server/authorization.mjs`（`projectCapabilities`，与 `projectPermissionDecision` 读同一张表）、`projectRoutes.mjs`（随 `/document` 读模型下发）、`src/lib/db.ts`（`cachedProjectCapabilities`）、工作流面板与评审面板 | `src/domain/projectCapabilities.test.ts`、`scripts/projectCapabilityContract.test.mjs`；**下发能力集合而不是角色** —— 界面拿角色再自己映射一遍权限就出现第二份权威，两份迟早漂移，表现是「按钮显示了但一点就 403」或更糟的「该藏的没藏」；**隐藏不是鉴权**，服务端始终是唯一边界，任何「反正界面藏了」的推论都是错的；能力未取到时保守缺省为 `['read']`（缺省成「什么都能做」会让一次读取失败变成越权入口全部显示）；入口→能力的对应是**声明式**的（`PROJECT_ENTRY_CAPABILITY`），散在组件里各写各的判断迟早漏判；契约测试锁住客户端词表与服务端权限表双向一致，并钉住 viewer ⊂ editor ⊂ owner 的包含关系与「未知角色不给任何能力」 |
| 项目记忆治理 | `server/botanicAgentMemory.mjs`、`src/domain/agentMemoryComparison.ts`（界面对比） | `botanicAgentPersistence.mjs`（`validateAgentMemoryEntity`）、`agentRoutes.mjs`（规划输入）、`botanicAgentContextTools.mjs`（`project_memory_search`）、`productionWorkflow.mjs`（`resolveWorkflowBrandRules`） | `server/botanicAgentMemory.test.mjs`、`botanicAgentPersistence.test.mjs`；**项目内只允许一条读取路径**，构造 Prompt/Plan/工作流定义都必须经选择器；`status` 是激活开关、`confidence` 是可信程度，不得互相顶替；active 需人工来源或已确认证据；已确认的人工规则不因措辞未命中而落选；冲突记忆不同时进同一 Plan 且落选可见；**适用主体 `subject`/`subjectValue`（Epic 6 §8.6）与 `scope` 是两个轴** —— `scope` 是包含范围、影响排序，`subject` 是适用条件、决定这条规则参不参与某一次生成。此前所有 active 记忆无差别进**每一次**生成，`scope` 只影响排序从不排除，于是「天猫顶部留 20% 安全区」会同样作用在京东项上，用户只能把「（仅天猫）」写进正文。四个维度来源都已存在（brand←`document.brandId`、product←批量项 `sku`、channel←批量项 `channel`、user←Run `ownerId`）；不适用时**排除而不是降权**（降权只是排后面，限额一满照样进 Prompt），落选原因进 `filtered` 并区分 `context_missing`（本次没有那个维度）与 `context_mismatch`（有但取值不同）—— 两者修法完全不同；限定主体却不给取值在**写入时**就拒绝；工作流**发布时固定全集**（`applySubjectFilter: false`，唯一允许关掉过滤的场景 —— 此刻还没有批量项、没有渠道可比，过滤会把规则挡在版本之外导致执行期再也挑不出来），执行期按 `item.input` 挑子集，因此「新版本不改变进行中的运行」仍成立；`brandRules` 与 `brandRuleBindings` 从同一份过滤后的列表派生以保证下标对应（此前各自 filter 会错位，按下标对应就会张冠李戴） |
| Skill 版本与生命周期 | `server/botanicAgentSkill.mjs` | `botanicAgentTools.mjs`（内置/项目 Skill 统一身份、真实风险、挂载依赖）、`agentRoutes.mjs`（创建时冻结 Manifest 与依赖、历史版本资源）、`productStoreContract.mjs` 与三个 Adapter、`src/domain/agent.ts` | `server/botanicAgentSkill.test.mjs`、`agentSkillAdapterContract.test.mjs`、`agentRoutes.test.mjs`；语义 hash 覆盖 name、instructions、capabilities 与完整 Manifest；每个版本保存完整不可变执行快照，修改必须无间隙追加版本，历史前缀不得截断/覆写，只有同一语义 draft→published 可补一次批准元数据；依赖创建时固定 Skill id/version/contentHash，DFS 只把当前递归栈判环，菱形依赖不误报；治理状态由流程产生，`published` 必须有批准人；Run 与挂载绑定 version/contentHash，内置 Skill 不例外；`manifest.toolAllowlist` 让风险取自称和注册表真实风险的较高者，未知工具按最高风险，少报能力在创建时拒绝；依赖缺失/弃用会显式进入 `dependencyIssues`；`guidance` 禁止虚假 outputSchema，`evaluator` 的结构化输出由评审执行器校验；`GET /api/projects/:projectId/agent-skills/:skillId/versions/:version` 按项目权限读取安全历史快照 |
| MCP Runtime V2 | `server/mcpClient.mjs`、`server/agentStructuredContract.mjs` | `botanicAgentTools.mjs`（安全目录、提案身份与执行绑定）、`agentActionExecution.mjs`、`agentActionReconciliationRoutes.mjs`、`agentRoutes.mjs`、`runtime.mjs` | `server/mcpClient.test.mjs`、`agentStructuredContract.test.mjs`、`botanicAgentTools.test.mjs`、`agentActionReconciliationRoutes.test.mjs`；目录只公开 key/version/capabilityHash/Schema/replay policy，不泄漏 URL/token；提案固定版本/hash，配置漂移 fail closed；输入/输出按受限对象 Schema 投影，JSON-RPC 2.0 id 严格匹配，响应先按字节限流；只有派发前校验/取消是 known failure，开始派发后的网络、取消、远端 error、协议与输出错误都是 `outcome_unknown` 且不得自动重放；外部 URL 不是媒体授权，只有同源 `/api/media/...` 可提升为 Artifact |
| 计划 Resolve 与 Compile | `server/creativePlanResolver.mjs` | `botanicCreativePlanCompiler.mjs`、`botanicAgentExecution.mjs`、`generationSubmissionService.mjs`、`botanicAgentRun.mjs`（`compiledPlanProvenance`） | `server/creativePlanResolver.test.mjs`、`botanicCreativePlanCompiler.test.mjs`、`botanicAgentExecution.test.mjs`；Resolve 是唯一读权威文档的一侧并抛阶段化错误，Compile 保持纯；plan 级指纹只能由 Resolve 统一计算，分支指纹由它派生；确认后的快照落在 Run 上，重试只读快照；快照与实际解析出的引用身份不一致时阻断而不换素材顶替 |
| Turn 与 Run 关联 | `run.turnId`（`botanicAgentRun.mjs`） | `productStore*.listAgentRunsForTurn`、`agentRoutes.mjs`（Turn 读模型 `linkedRunIds`）、`src/domain/agentPlanContract.ts` | `server/productStore.test.mjs`、`agentRoutes.test.mjs`、`botanicAgentRun.test.mjs`；权威边只在 Run 上，Turn 侧读时派生（Turn 记录会被整条覆盖写）；反查按 Turn 走存储查询，不得列全部 Run 再本地过滤 |
| 取消生成与成本停止 | `server/generationCancellation.mjs` | `generationCancelCapability.mjs`、`generationJobExecution.mjs`、`localCancelRegistry.mjs`、`agentRunEventBus.mjs`（`botanic-agent-cancels`）、`generationProcessor.mjs`、`agentCancellationService.mjs`、`worker.mjs`、`src/domain/generationCancelCopy.ts` | `server/generationCancellation.test.mjs`、`generationJobExecution.test.mjs`、`generationProcessorFence.test.mjs`、`agentCancellationService.test.mjs`、`src/domain/generationCancelCopy.test.ts`；判定按取消前状态算，出队只对未派发有效；Redis publish/abort 不等于 Worker 已退出，Processor 结束 Provider/heartbeat 并释放 registry 后才按 signalId+generation ack，崩溃仅在 DB lease 过期后代确认；ack 前 Turn 保持 cancelling；无 Provider 支持远端停止计费，文案不得暗示取消等于省钱 |
| 版本化生产工作流 | `server/productionWorkflow.mjs` | `productionWorkflowRoutes.mjs`、`productionWorkflowAdvance.mjs`、`deliveryManifest.mjs`、`src/domain/workflowBatchInput.ts`（CSV 导入 + 表格编辑）、`generationSubmissionService.mjs`、`src/domain/productionWorkflows.ts`、`src/lib/productionWorkflowApi.ts`、模板面板、Artifact Index | 已验证 Agent/画布操作可提升为版本；版本固定计划指纹、Skill/Memory 绑定与质量策略，新版本不影响进行中的运行；批量项标识来自业务身份（SKU/渠道/语言）而非位置；运行推进在 Worker 侧周期对账（`workflow.advance`），页面无人打开也能收口，但状态收敛沿用 `applyWorkflowItemResult` 那一份判定；交付清单只含人工批准过的候选，被排除项与原因一并给出，文件名冲突拒绝出包；版本固定的品牌规则以执行契约前缀进入 Prompt（不拼进画面描述）；CSV 导入自己解析引号与换行，列数不符与重复标识**报告**而不是补空或丢弃；**批量输入界面**（`CanvasWorkspacePanels` 的 `production-batch`）此前发的一直是写死的 `items: [{ id: 'item-1' }]`，整条批量能力从界面上用不到 —— 现在 CSV 粘贴导入 + 可增删改的表格，编辑时**实时**重查重复标识并逐行标出原因（只在提交时报「有错」的话，用户不知道该改哪一行，而提交之后钱已经花出去了）；清空单元格删除该键而不留空串（空串会被当成「声明了该字段且值为空」，插值出空白 Prompt）；一行都没有时退回单项运行，不逼用户先填一行空表格 |
| 批量变化 | `src/domain/batchVariations.ts` | `src/store/canvasBatchVariationActions.ts`、服务端 Processor | `batchVariations.test.ts`、Processor 测试；计划先限制总输出，Store 以有界并发协调独立子任务及恢复，各分支独立持久化 |
| Agent 对话分流 | `src/domain/agentChatContract.ts` | `src/lib/agentApi.ts`、`server/botanicAgentChat.mjs`、`server/agentWebResearch.mjs`、`webSearchProvider.mjs` | 对话测试；浏览器不发送图片字节或私有 URL；Tavily 为默认 `web_search`；URL 守卫与结果清洗只在服务端一份实现 |
| Agent 工具流式时间线 | `src/domain/agentTimeline.ts`、`src/domain/agentChatStream.ts` | `server/agentToolRuntime.mjs`、`/api/agent-turns/stream`、兼容 `/api/agent-chat|intent|plans/stream` Adapter、`AgentWorkspace.tsx`、`AgentToolOrb.tsx` | 四个流入口共用 durable accepted/observer；工具步仅在 `registry.execute` 前后 emit；禁止客户端 rAF 假进度；确认后 Run/分支投影进同款时间线；`agentTimelineOrbState` 只映射动画态 |
| Agent 计划和执行 | `src/domain/agentPlanContract.ts` | `agent.ts`、`agentVariations.ts`、`agentInstructionRouting.ts`、`server/botanicAgentPlanner.mjs`、`botanicAgentTools.mjs`、`agentRunGenerationService.mjs`、`agentRunSubmissionSweep.mjs`、`derivedTaskQueue.mjs`（`run.submit`）、`worker.mjs` | Agent Planner/Tool/Run 测试、`server/agentRunSubmissionSweep.test.mjs`、`scripts/agentRunSubmissionWorkerContract.test.mjs`；无素材组时按已确认变体轴展开，张数由展开结果决定；Run 生成复用持久化幂等任务，外部行动默认确认；queued Run 无首个 Job 时稳定分页恢复，复用提交与深取消服务，不复制 Job 规则；镜像行为由 `scripts/fixtures/agentVariationMirrorCases.json` 两侧对拍锁定 |
| Agent 多模态视觉 | `server/botanicAgentVision.mjs`、`botanicAgentVisionCapability.mjs` | `botanicAgentChat.mjs`、`botanicAgentTurn.mjs`、`runtime.mjs`（`AGENT_VISION_MODEL` 只做 caption） | 视觉测试；所选看图模型（Gemini 3.7 / Vision Exp）原生多模态；纯文本选型走 caption + 所选模型；识别结果不进任何持久化实体 |
| 局部重绘（选区→蒙版） | `src/domain/regionMask.ts` | `server/regionMaskPng.mjs`、`imageOverlay.mjs`、`generationProvider.mjs`、`generationComposition.mjs`、`RegionMaskEditor.tsx`、`agent.ts`（`region_edit`） | regionMask/overlay/provider/composition 测试；选区是归一化纯数据；贴标识默认走 GPT Image 2 高质 edits，像素贴图层仅 `composeMode=overlay` |
| MCoT 创意分解 | `src/domain/agentCreativeComposition.ts` | `server/botanicAgentTurn.mjs`（`decompose_creative_brief`）、`AgentConversationMessage.tsx`（方案卡）、`agentVariations.ts`（条目分支）、`botanicAgentExecution.mjs` | 分解/持久化/执行测试；成套请求落成 `kind: composition` 消息与方案卡，刷新后从消息恢复「生成第 N 项 / 执行方案」；方案卡逐项/整套按钮绑定该条消息自己的 composition |
| Agent 可观测与评测 | `server/executionTelemetry.mjs`、`server/agentTraceContext.mjs`、`server/agentSemanticEvent.mjs` | `botanicTelemetry.mjs`、三类 Queue、`agentContextObservability.mjs`、`agentSemanticMetrics.mjs`、`agentRunObservability.mjs`（legacy 双写）、`agentExecutionTrace.mjs`（产品聚合视图）、`agentOperationalMetrics.mjs`、`agentEvalSuite.mjs`、`scripts/evalGate.mjs` | W3C traceparent 跨 HTTP/Generation/Derived/Subagent 队列传播，Worker Handler 看不到 carrier；baggage 禁止，外部 Provider/MCP 只发 traceparent；OTel traces 默认关闭，GenAI development semconv 独立关闭；Span/semantic schema 只允许 ID、计数、耗时和错误码，不采集 Prompt/body/reasoning/媒体 URL；旧 `agent-trace:*` 是产品实体聚合 ID，不伪装 W3C trace ID；Context active/shadow/control/killed 有安全事件与无样本 null 指标；发布 Gate 仍只跑固定夹具，不调用真实 Provider |
| Agent 权限与审批 | `server/agentActionGovernance.mjs` | `authorization.mjs`、`projectAuthorization.mjs`、`agentRoutes.mjs`、`projectRoutes.mjs` | 服务端权限矩阵、短期行动审批和脱敏审计导出；UI 不可替代鉴权 |
| Agent 持久化 | `server/botanicAgentPersistence.mjs` | 三个 ProductStore Adapter、Canvas 兼容视图 | 独立实体合并测试；Memory 墓碑永久胜出 |
| Agent 协作历史 | `server/collaborationActivityPersistence.mjs` | `agentRoutes.mjs`、三个 ProductStore Adapter、`agentRunEventBus.mjs`、`useCanvasWorkspaceSynchronization.ts` | 稳定游标分页、成员回执单调前进、跨实例实时失效；旧快照不得覆盖远端独立实体 |
| Artifact Index | `server/botanicArtifactIndex.mjs` | 三个 Store Adapter、Agent 结果区 | Artifact 测试和迁移对账；历史不随 UI 删除 |
| 素材与媒体 | `src/domain/asset*.ts`、`agentMedia.ts` | `src/lib/db.ts`、`server/mediaService.mjs`、`objectStore.mjs` | 素材/媒体测试；组件不接触对象存储凭据 |
| 媒体格式词表与像素守卫 | `server/mediaFormats.mjs` | `src/domain/mediaFormats.ts`（客户端副本，架构门禁禁止 `src/` 导入 `server/`）、`mediaService.mjs`、`generationProvider.mjs`（`assertImagePixelBudget`）、`imageOverlay.mjs`、`src/lib/agentApi.ts` | `server/mediaFormats.test.mjs`、`src/domain/mediaFormats.test.ts`、`scripts/mediaFormatContract.test.mjs`；格式词表与字节嗅探只有一份权威实现，两侧副本由契约测试锁一致；`accept=`、格式清单文案由词表派生不手写；校验仍在服务端（`accept=` 是提示不是边界），参考图与蒙版按像素总数设上限，dataUrl 提交时即拦、mediaId 留 Worker 兜底是预期不对称 |
| 粘贴图片（画布/对话框） | `src/domain/clipboardMedia.ts` | `useCanvasInteractionCoordinator.ts`（画布落点）、`AgentWorkspace.tsx`（composer 落点）、`src/lib/uploadedAssets.ts`（粘贴来源命名回落） | `src/domain/clipboardMedia.test.ts`、`e2e/paste-media.spec.ts`；无媒体文件一律 ignore——文本粘贴绝不被劫持；模态弹层打开时画布粘贴忽略，素材不落在弹层背后看不见的地方；焦点在对话框内粘贴归 composer；文件名无意义时用带时间戳的双语回落名 |
| 项目同步 | `src/lib/db.ts` | `projectRealtime.ts`、`projectCollaboration.ts`、`server/realtimeHub.mjs`、`server/canvasRealtimeEventBus.mjs`、Store | Realtime/冲突/双实例测试；`revision` 与 `graphRevision` 分工明确，跨实例 Yjs 只在来源实例落库 |
| 账户与权限 | `src/lib/productSession.ts` | `server/authorization.mjs`、`projectAuthorization.mjs` | 授权和账户测试；越权 403、真实缺失 404 |
| HTTP 路由 | `server/httpRouteTable.mjs` | `sessionRoutes.mjs`、`projectRoutes.mjs`、`generationRoutes.mjs`、`accountRoutes.mjs`、`libraryRoutes.mjs`、`agentRoutes.mjs`、`promptMediaRoutes.mjs`、`realtimeTicketRoutes.mjs` | 资源模块返回是否已处理并拥有 405/Allow；组合根只负责鉴权基础设施与处理器编排 |
| ProductStore | `server/runtime.mjs` | `productStore.mjs`、`postgresProductStore.mjs`、`supabaseProductStore.mjs` | Adapter 契约及各 Store 测试 |
| 投放交付 | `src/domain/deliveryPresentation.ts` | `src/lib/deliveryExport.ts` | delivery 测试；视频不进入图片投放模板 |

## 依赖方向

```text
App / Feature UI → Store → Domain + Browser Lib → Node HTTP → Queue / Processor → Adapter
```

- `src/domain/`：纯规则和数据契约，不依赖 UI、Store、网络或存储。
- `src/lib/`：浏览器网络与本地持久化 Adapter，不依赖 UI 或 Store。
- `src/store/`：组合领域规则和浏览器 Adapter，不依赖 UI。
- `src/components/`：纯展示和用户事件，只依赖领域类型与共享 UI。
- `src/features/`：按产品能力组合 Store、Lib 和组件；对外暴露一个明确功能入口。
- `src/App.tsx`：应用壳，只负责登录恢复和功能模块加载，不拥有画布或领域规则。
- `src/domain/latestOperation.ts`：跨 Store/功能 UI 复用的最后请求令牌，项目切换时显式失效旧结果。
- `src/features/agent/useAgentMessageDelivery.ts`：Agent 消息本地追加、离线排队与联网重放的单一入口。
- `src/features/agent/AgentConversationMessage.tsx` 与 `AgentComposer.tsx`：分别拥有消息/计划卡展示和输入区交互，避免工作区编排器继续吸收视图细节。
- `src/store/canvasGenerationLifecycle.ts`：从持久化任务重建请求与 UI 生命周期状态的单一入口；不从本地 loading 推断任务真相。
- `src/store/canvasAgentActions.ts`：保持 `CanvasStore` 接口不变，集中 Session、Message、Memory 与 Run 的兼容提交命令。
- `server/runtime.mjs`：服务端组合根，选择 ProductStore、队列、媒体和 Provider Adapter。

完整规则见 [ARCHITECTURE.md](ARCHITECTURE.md)，自动检查见 `scripts/architectureBoundaries.mjs`。

## 检索建议

优先搜索领域名和公开命令，不要从 CSS 类名反推业务：

```bash
rg "runGraphGeneration|runBatchVariation" src server
rg "BotanicAgentRun|AgentArtifact" src/domain server
rg "requireProjectPermission" server
rg "revision|graphRevision" src/lib server
```

如果需求同时命中 App、Store 和服务端，先固定跨层接口与不变量，再逐层修改；不要在一次 UI 修改中顺带改变任务或持久化语义。

## 验证矩阵

| 改动范围 | 必跑验证 |
| --- | --- |
| Domain / Lib | 对应 `node --test ...`，然后 `npm test` |
| UI / Store | 对应行为测试、`npm run build`、必要的浏览器回归 |
| Server / Adapter | 对应 `server/*.test.mjs`、`npm test` |
| 依赖方向 | `npm run check:architecture` |
| 发布相关 | 以上全部，加生产浏览器、控制台、HTTP 与 Provider 分项验证 |

UI E2E 门禁使用本地持久化与伪健康接口，覆盖项目 → 画布 → Agent、面板互斥、Composer 执行模式、`/` Skill 与 `@` 引用和 hash 刷新恢复；不消耗真实 Provider 额度。
